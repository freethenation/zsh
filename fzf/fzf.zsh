# Setup fzf
# ---------
export PATH="$PATH:$HOME/.zsh/fzf"

# Auto-completion
# ---------------
#[[ $- == *i* ]] && source "$HOME/.zsh/fzf/completion.zsh" 2> /dev/null

# Key bindings
# ------------
source "$HOME/.zsh/fzf/key-bindings.zsh"

